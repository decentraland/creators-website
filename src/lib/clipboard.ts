// Host-facing side effect (see CONVENTIONS.md): the one place the app touches the clipboard.
export async function copyToClipboard(text: string): Promise<boolean> {
  try {
    await navigator.clipboard.writeText(text)
    return true
  } catch {
    return false
  }
}
